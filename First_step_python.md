##  LEVEL 1 — Already Learned / Warm-up

### Conditions

1. Check even or odd
2. Check positive, negative, or zero
3. Check whether a number is divisible by 5
4. Find the larger of two numbers
5. Find the largest of three numbers
6. Check pass/fail from marks
7. Grade calculator
8. Check whether a number is divisible by both 3 and 5
9. Check whether a year is a leap year
10. Check whether a person is eligible based on age

### Loops

11. Print `1` to `N`
12. Print `N` to `1`
13. Print even numbers `1` to `N`
14. Print odd numbers `1` to `N`
15. Find total numbers from `1` to `N`
16. Find sum `1` to `N`
17. Find sum of even numbers
18. Find sum of odd numbers
19. Count numbers divisible by `3`
20. Count numbers divisible by `5`

---

#  LEVEL 2 — Basic Logic Building

Now we start combining **loop + condition + counter/accumulator**.

21. Count even and odd numbers between `1` and `N`.

```text
Input: 20

Even: 10
Odd: 10
```

22. Count numbers divisible by both `3` and `5`.

23. Find the sum of numbers divisible by `3`.

24. Find the sum of numbers divisible by `3` or `5`.

25. Find the largest number between `1` and `N`.

26. Find the smallest number between `1` and `N`.

27. Count numbers greater than `50` between `1` and `N`.

28. Print numbers between `1` and `N` that are divisible by `2` but not by `4`.

29. Find the total of numbers that are both even and divisible by `3`.

30. From `1` to `N`, find:

```text
Total
Even count
Odd count
Even sum
Odd sum
```

---

#  LEVEL 3 — `while` Loop

This is your next major gap.

31. Print `1` to `N` using `while`.

32. Print `N` to `1` using `while`.

33. Print even numbers using `while`.

34. Print odd numbers using `while`.

35. Find sum from `1` to `N` using `while`.

36. Count numbers divisible by `3` using `while`.

37. Find factorial using `while`.

38. Reverse a number using `while`.

39. Count digits of a number.

```text
Input: 58392
Output: 5
```

40. Find sum of digits.

```text
Input: 58392
Output: 27
```

---

#  LEVEL 4 — Digit Logic

Now things get more interesting.

41. Find the largest digit.

```text
Input: 58392
Output: 9
```

42. Find the smallest digit.

43. Count even digits.

44. Count odd digits.

45. Count how many times `5` occurs.

46. Find product of digits.

47. Find the first digit.

48. Find the last digit.

49. Remove the last digit repeatedly and print each digit.

50. Check whether all digits are even.

---

#  LEVEL 5 — `break` and `continue`

Now learn to control loops.

51. Print numbers `1–20`, but stop at `10`.

52. Search for number `7` in `1–20` and stop when found.

53. Print `1–20` but skip `10`.

54. Print only numbers that are **not divisible by 3**.

55. Print numbers from `1–100`, but stop when you reach the first number divisible by `17`.

56. Find the first number between `1` and `N` divisible by both `7` and `11`.

57. Keep taking numbers from the user until they enter `0`.

58. Keep taking numbers until the user enters a negative number.

59. Print numbers from `1–100`, skipping multiples of `5`.

60. Search for the first prime number after `N`.

---

#  LEVEL 6 — Number Problems

Now you start becoming genuinely comfortable with logic.

61. Check whether a number is prime.

62. Print all prime numbers from `1` to `N`.

63. Count prime numbers from `1` to `N`.

64. Find the sum of prime numbers from `1` to `N`.

65. Find all factors of a number.

66. Count factors.

67. Find sum of factors.

68. Find the largest factor excluding the number itself.

69. Check whether a number is perfect.

```text
6 → 1 + 2 + 3 = 6
```

70. Find all perfect numbers between `1` and `N`.

---

#  LEVEL 7 — Mathematical Logic

71. Factorial.

72. Calculate `a^b` without using `**`.

73. Find GCD.

74. Find LCM.

75. Check Armstrong number.

76. Print Armstrong numbers from `1` to `N`.

77. Check strong number.

78. Check perfect number.

79. Check whether a number is a Harshad/Niven number.

80. Find the digital root.

Example:

```text
9875

9 + 8 + 7 + 5 = 29
2 + 9 = 11
1 + 1 = 2

Output: 2
```

---

#  LEVEL 8 — Reverse & Palindrome

This level is extremely useful for your logic.

81. Reverse a number.

82. Check number palindrome.

83. Check string palindrome.

84. Find whether the first and last digits are the same.

85. Reverse only the even digits.

86. Reverse only the odd digits.

87. Find the difference between original and reversed number.

88. Check whether a number becomes palindrome after reversing.

89. Find all palindrome numbers from `1` to `N`.

90. Find the largest palindrome below `N`.

---

#  LEVEL 9 — Fibonacci & Series

91. Print first `N` Fibonacci numbers.

92. Find the Nth Fibonacci number.

93. Find sum of first `N` Fibonacci numbers.

94. Print only even Fibonacci numbers.

95. Print only odd Fibonacci numbers.

96. Count Fibonacci numbers below `N`.

97. Check whether a number belongs to Fibonacci sequence.

98. Print Fibonacci numbers between two limits.

99. Generate:

```text
1
11
121
1231
12341
```

using logic.

100. Create your own number series and identify the pattern.

---

#  LEVEL 10 — Nested Loop Logic

Now we introduce serious loop thinking.

101. Print:

```text
*
**
***
****
*****
```

102.

```text
*****
****
***
**
*
```

103.

```text
1
12
123
1234
12345
```

104.

```text
1
22
333
4444
55555
```

105.

```text
12345
1234
123
12
1
```

106.

```text
    *
   **
  ***
 ****
*****
```

107. Print a square.

```text
*****
*****
*****
*****
*****
```

108. Print a hollow square.

109. Print a right triangle.

110. Print an inverted triangle.

---

#  LEVEL 11 — Real Logic Problems

Now **I stop telling you which concept to use.**

111. Find the second-largest number.

112. Find the second-smallest number.

113. Find duplicate digits in a number.

114. Find the most frequent digit.

115. Find the least frequent digit.

116. Find whether all digits are unique.

117. Find the missing number from `1` to `N`.

118. Find two numbers whose sum equals a target.

119. Find the largest pair product.

120. Find the longest sequence of consecutive numbers.

---

#  LEVEL 12 — Mixed Problems

These are the problems that tell us whether your logic is actually improving.

121. Take `N` numbers from the user and find:

```text
Total
Even count
Odd count
Positive count
Negative count
Largest
Smallest
Average
```

122. Keep accepting numbers until `0` is entered and calculate the above statistics.

123. Keep accepting numbers until the user enters a negative number.

124. Create a number guessing game.

125. Create a simple calculator using:

```text
+
-
*
/
%
```

126. Create a menu-driven calculator.

127. Create a simple ATM simulation.

128. Create a student marks calculator.

129. Create a simple login system with 3 attempts.

130. Create a number-based mini game.



